name: Events Example

on:
  pull_request:
    types:
      - closed


jobs:
  printHelloWorld:
    runs-on: ubuntu-latest
    steps: 
      - name: first step
        run: echo "Hello World from events"

        